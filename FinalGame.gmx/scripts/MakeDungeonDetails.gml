//Create whatever auto-dialogues and other stuff that is supposed to exist in the dungeon right now.

if room == rm_dungeon_test2 {
    TryAutoDialogue('d1a1', true)
}

if room == rm_cave {
    TryAutoDialogue('d2a1', true)
}
